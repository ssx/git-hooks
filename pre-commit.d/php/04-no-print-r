#!/usr/bin/env bash
#
# Check: no print_r() calls
# Scope: php

tmp="$1"
src="$2"

if matches=$(grep -nE '\bprint_r[[:space:]]*\(' "$tmp" 2>/dev/null); then
  printf '%s\n' "$matches" | sed "s|^|        $src:|"
  exit 1
fi

exit 0
